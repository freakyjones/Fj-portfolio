---
trigger: always_on
---

# Next.js & React Rules

## Architecture & Code Style
- Use TypeScript for all files.
- Prefer React Server Components (RSC) by default. Use `"use client"` only when interactive features (state, effects, event listeners) are needed.
- Keep components small, focused, and modular.
- Organize assets and components cleanly within the `src/` directory.

